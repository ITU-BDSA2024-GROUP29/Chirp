@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="cheepbox">
    <h3>What's on your mind @(User.Identity.Name)?</h3>
    <form asp-page-handler="Cheep" method="post">
        @Html.AntiForgeryToken()
        <input style="float: left" type="text" name="Post" placeholder="Type your message..." maxlength="160" />
        <button type="submit">Share</button>
    </form>
</div>
<style>
</style>